<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application1.coys.schoolcomms">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".googleSheet" android:parentActivityName = ".MainActivity"  />
        <activity
            android:name=".bookRoom"
            android:parentActivityName = ".MainActivity"
            android:label="@string/title_activity_book_room"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Intercom" android:parentActivityName = ".MainActivity" />
        <activity android:name=".DailyNotes" android:parentActivityName = ".MainActivity" />
        <activity android:name=".ITsupport" android:parentActivityName = ".MainActivity" />
        <activity android:name=".MessageAdapter" />
        <activity android:name=".FriendlyMessage"  />
        <activity android:name=".AddItem" android:parentActivityName = ".bookRoom"/>
    </application>

</manifest>